# PB.ai 환경변수 예시 — 실제 값은 .env.local 에 작성하세요

# mock 데이터 사용 여부. 'false'로 두면 실제 백엔드 API 호출 (기본: true)
NEXT_PUBLIC_USE_MOCK_DATA=true

# 백엔드 API 베이스 URL (/api/v1 자동 부착)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
